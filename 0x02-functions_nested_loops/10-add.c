#include "main.h"

/**
 * add - returns the sum of two numbers
 * @c: parameter to be added
 * @d: parameter to be added
 * Return: the sum
 */

int add(int c, int d)
{
	int sum;

	sum = c + d;
	return (sum);
}
