#include "main.h"

/**
 * _abs - checks absolute value for an integer
 * @n: checks the character
 * Return: absolute value
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
